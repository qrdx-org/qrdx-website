{
  "name": "qrdx-website",
  "version": "1.0.0",
  "description": "A modern React website with routing built using esbuild",
  "main": "src/index.jsx",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/build.js",
    "start": "node scripts/dev-server.js",
    "clean": "rm -rf dist node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["react", "esbuild", "router", "spa"],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.6.4",
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.9.5"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
